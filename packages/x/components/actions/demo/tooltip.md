## zh-CN

通过设置 `tooltip` 属性可以自定义操作项的提示内容，也可以传入 `TooltipProps` 配置或设置为 `false` 关闭 Tooltip。

## en-US

Use the `tooltip` property to customize the tooltip content, pass `TooltipProps` for additional configuration, or set it to `false` to disable the tooltip.
