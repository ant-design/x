## zh-CN

通过 `confirmable` 属性显示确认按钮，用户完成选择后点击确认触发 `onConfirm` 回调。

## en-US

Use the `confirmable` property to show a confirm button. After users finish selecting, click confirm to trigger the `onConfirm` callback.
