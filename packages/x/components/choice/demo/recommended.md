## zh-CN

通过 `recommended` 属性标记 AI 推荐选项。支持 `true`/`'primary'`（主推荐）和 `'secondary'`（次推荐）。

## en-US

Use the `recommended` property to mark AI recommended options. Supports `true`/`'primary'` (primary recommendation) and `'secondary'` (secondary recommendation).
