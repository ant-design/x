## zh-CN

使用 `expanded` 和 `onExpandedChange` 完全控制详情区域，适合与页面检查模式或全局偏好联动。

## en-US

Use `expanded` and `onExpandedChange` to fully control the details area and synchronize it with an inspection mode or global preference.
